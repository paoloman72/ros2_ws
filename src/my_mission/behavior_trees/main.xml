<?xml version="1.0"?>

<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <!--
      Template for your real mission. It must be FINITE: it ends in SUCCESS
      or FAILURE (the simulator stops the robot after a successful run).
      For navigation node references (WaitForRobotReady, NavigateToPose,
      PrintNavigationPath...) see custom_bt_example_success/.
    -->
    <Sequence name="MainTree">
      <GreetNode who="mission"/>

      <ExampleNode message="mission started"/>

      <!-- TODO: your mission steps -->

      <GreetNode who="mission complete"/>
    </Sequence>
  </BehaviorTree>
</root>
